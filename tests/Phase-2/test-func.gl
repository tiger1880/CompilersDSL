$ function definition

func void _ola9(int a, line b, regPoly p) {

    int a := 1
    int b := 10

    until (a < b)
    {
        a +:= (b-a)/2
    }

    return 
}

func label fullName(label firstName, label lastName) {

    label c := firstName + " " + lastName
    return c
}

$ statements outside are also allowed
int c := 10
c := c*10

func real distOfIntersection(line l1, line l2)
{
    
    if (l1 || l2) {
        return -1
    }

    point p := INTERSECTION(l1, l2)

    return ||p||
    
}
